<h1>Gallery</h1>

<p>view photos and videos here</p>

<div><%= link_to 'New Project', new_project_path(@project_item) %></div>

  <% @project_item.each do |project| %>
<p><%= link_to project.title, project_show_path(project) %></p>
  <% end %>